import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

//we will simply call app.jsx to render whatever we want to show
ReactDOM.render(<App />, document.getElementById("root"));
